import WishListContainer from "../../components/wishList.components/wishlist.component";
import styled  from 'styled-components';

const {div} = styled

export const WishListContainerStyle = styled(WishListContainer)`
        display: flex;
         padding: 83px;
        margin-bottom: 51px;
        flex-wrap: wrap;
        padding-top: 20px;
`